{
  "version": 2,
  "builds": [
    { "src": "bin/www", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "bin/www" }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "scripts": {
    "build": "npm install && npm run build",
    "start": "node bin/www"
  }
}